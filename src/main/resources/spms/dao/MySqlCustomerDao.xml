<?xml version="1.0" encoding="UTF-8"?>
<!-- 2. 동적 SQL 적용 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="spms.dao.CustomerDao">
	<resultMap type="customer" id="ResultMap">
		<id column="CUSTOMER_INDEX" property="index" />
		<result column="CUSTOMER_EMAIL" property="email" />
		<result column="CUSTOMER_PASSWORD" property="password" />
		<result column="CUSTOMER_NAME" property="name" />
		<result column="CUSTOMER_BIRTH" property="birth" />
		<result column="CUSTOMER_PHONENUM" property="phonenum" />
		<result column="CUSTOMER_ZIPCODE" property="zipcode" />
		<result column="CUSTOMER_ADDRESS" property="address" />
		<result column="CUSTOMER_DETAILADDRESS" property="detailaddress" />
		<result column="CUSTOMER_ACCESS" property="access" />
	</resultMap>

	<select id="selectList" parameterType="map"
		resultMap="ResultMap">
		SELECT CUSTOMER_INDEX,CUSTOMER_EMAIL,CUSTOMER_PASSWORD,CUSTOMER_NAME,CUSTOMER_BIRTH,CUSTOMER_PHONENUM,CUSTOMER_ZIPCODE,CUSTOMER_ADDRESS,CUSTOMER_DETAILADDRESS,CUSTOMER_ACCESS
		FROM CUSTOMER
	</select>
	
	<insert id="insert" parameterType="customer">
		INSERT INTO CUSTOMER (CUSTOMER_EMAIL,CUSTOMER_PASSWORD,CUSTOMER_NAME,CUSTOMER_BIRTH,CUSTOMER_PHONENUM,CUSTOMER_ZIPCODE,CUSTOMER_ADDRESS,CUSTOMER_DETAILADDRESS,CUSTOMER_ACCESS)
		VALUES(#{email},#{password},#{name},#{birth},#{phonenum},#{zipcode},#{address},#{detailaddress},1)
	</insert>
	
	<select id="selectOne" parameterType="int"
		resultMap="ResultMap">
		SELECT CUSTOMER_INDEX,CUSTOMER_EMAIL,CUSTOMER_PASSWORD,CUSTOMER_NAME,CUSTOMER_BIRTH,CUSTOMER_PHONENUM,CUSTOMER_ZIPCODE,CUSTOMER_ADDRESS,CUSTOMER_DETAILADDRESS,CUSTOMER_ACCESS
		FROM CUSTOMER
		WHERE CUSTOMER_INDEX=#{value}
	</select>
	
	<update id="update" parameterType="map">
		UPDATE CUSTOMER
		<set>
			<if test="email != null">CUSTOMER_EMAIL=#{email},</if>
			<if test="password != null">CUSTOMER_PASSWORD=#{password},</if>
			<if test="name != null">CUSTOMER_NAME=#{name},</if>
			<if test="birth != null">CUSTOMER_BIRTH=#{birth},</if>
			<if test="phonenum != null">CUSTOMER_PHONENUM=#{phonenum},</if>
			<if test="access != null">CUSTOMER_ACCESS=#{access},</if>
		</set>
		WHERE CUSTOMER_INDEX=#{index}
	</update>
	
	<select id="exist" parameterType="map"
		resultMap="ResultMap">
		SELECT CUSTOMER_INDEX,CUSTOMER_EMAIL,CUSTOMER_PASSWORD,CUSTOMER_NAME,CUSTOMER_BIRTH,CUSTOMER_PHONENUM,CUSTOMER_ZIPCODE,CUSTOMER_ADDRESS,CUSTOMER_DETAILADDRESS,CUSTOMER_ACCESS 
		FROM CUSTOMER
		WHERE CUSTOMER_EMAIL=#{email} and CUSTOMER_PASSWORD=#{password}
	</select>
	
	<select id="finder" parameterType="map"
		resultMap="ResultMap">
		
		<if test="email == null">
		SELECT CUSTOMER_EMAIL 
		FROM CUSTOMER
		WHERE CUSTOMER_NAME=#{name} AND CUSTOMER_BIRTH=#{birth} AND CUSTOMER_PHONENUM=#{phonenum}
		</if>
		
		<if test="email != null">
		SELECT CUSTOMER_PASSWORD 
		FROM CUSTOMER
		WHERE CUSTOMER_EMAIL=#{email} AND CUSTOMER_NAME=#{name} AND CUSTOMER_BIRTH=#{birth} AND CUSTOMER_PHONENUM=#{phonenum}
		</if>
		
		LIMIT 1
	</select>
	
	<select id="email" parameterType="map"
		resultMap="ResultMap">
		SELECT CUSTOMER_INDEX
		FROM CUSTOMER
		WHERE CUSTOMER_EMAIL=#{email}
	</select>
</mapper>